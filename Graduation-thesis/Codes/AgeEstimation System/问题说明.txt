视图更新的问题在于：

在估计的时候没有问题，但是最小化，或者被遮盖后重新绘制的时候有问题，因为会使默认的sexFlag来刷新图片，每次读入图像时会产生一个新的sexFlag，刷新的时候Paint函数中就是根据这个sexFlag来判断男性还是女性，导致出错了，使用的是新读入图像的性别来重新绘制全部的图片上面的性别，当然，年龄没有影响。


解决办法是：存储下来这个性别，以后重绘的时候就根据这个性别来显示，由于是存储下来的，所以根本没有错误。

注意：本版本V3.1通过测试发现已经完美的解决了视图更新的问题，但是，由于性别估计仍然会存在问题，所以还是会产生误差。

